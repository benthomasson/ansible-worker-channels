============
Installation
============

At the command line::

    $ easy_install ansible-worker-channels

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ansible-worker-channels
    $ pip install ansible-worker-channels
